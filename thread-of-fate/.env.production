# Production build base path. vite.config.ts builds `base` as
# `${VITE_SHEET_PATH}/${VITE_SHEET_SHORT_NAME}/`. For a Roll20 Beacon GitHub Page
# served at https://<user>.github.io/<repo>/, set the short name to the repo name.
VITE_SHEET_PATH=
VITE_SHEET_SHORT_NAME=thread-of-fate
